import React, { Component } from 'react';

const writeUps = [
{name:"Juiceline", location: "Budapest, Hungary", descrip: "A progressive web app that allows customers to see what's offered at the current moment.", stack: ["Figma", "React", "JS", "CSS3", "Sass"]}
];

class WriteUp extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default WriteUp;